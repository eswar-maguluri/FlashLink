global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "flashlink-api"

    metrics_path: /metrics

    scheme: https

    static_configs:
      - targets:
          - flashlink-api.onrender.com